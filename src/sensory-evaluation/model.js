export class Tasting {
  constructor({ id, session, notes, score }) {
    this.id = id;
    this.session = session;
    this.notes = notes;
    this.score = score;
  }
}
// Agrega aquí otras entidades SOLO si realmente se usan en el contexto de sensory-evaluation. 